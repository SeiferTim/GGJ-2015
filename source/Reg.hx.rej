diff a/source/Reg.hx b/source/Reg.hx	(rejected hunks)
@@ -25,7 +25,6 @@
 		
 		defineItems();
 		
-		game = new Game(Players + 2, Turns == 0 ? 10 : Turns == 1 ? 20 : 30);
 		game = new Game(Players + 2, Turns == 0 ? 15 : Turns == 1 ? 30 : 45);
 		
 		availableRaces = [0, 1, 2, 3];
